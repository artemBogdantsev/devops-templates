apiVersion: cert-manager.io/v1beta1
kind: Certificate
metadata:
  name: service-usercentrics-sandbox-cert
  namespace: istio-system # alongside the ingress object
spec:
  secretName: service-usercentrics-sandbox-tls
  dnsNames:
  - '*.service.usercentrics-sandbox.eu'
  issuerRef:
    name: letsencrypt-issuer-dev
    kind: ClusterIssuer